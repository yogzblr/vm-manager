apiVersion: v1
kind: Secret
metadata:
  name: control-plane-secrets
  namespace: vm-manager
  labels:
    app.kubernetes.io/name: control-plane
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # These should be replaced with actual secrets in production
  # Use sealed-secrets or external-secrets in production
  database-password: "changeme"
  jwt-secret: "change-this-to-a-secure-random-string-at-least-32-characters"
